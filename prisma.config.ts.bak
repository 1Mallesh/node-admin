import { defineConfig } from "prisma/config";

export default defineConfig({
  datasource: {
    db: {
      provider: "mongodb",
      url: process.env.MONGO_URI!, // Make sure this is set in .env
    },
  },
});
